import type { IconProps, IconOptions } from 'vuetify'

export interface HcIconProps extends IconProps {
  fill?: boolean | string
  size?: string | number
}

export interface HcIconOptions extends IconOptions {
  size?: string | number
}
 